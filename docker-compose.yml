services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: final_shop
    ports:
      - "5432:5432"
    volumes:
      - final_shop_db:/var/lib/postgresql/data

volumes:
  final_shop_db:
